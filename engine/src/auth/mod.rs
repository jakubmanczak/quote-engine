// use sqlx::{Pool, Sqlite};

pub mod error;

pub const AUTH_COOKIE_NAME: &str = "qauth";

// pub async fn auth_via_credentials(pool: &Pool<Sqlite>) -> () {
//     //
// }

// pub async fn auth_via_session(pool: &Pool<Sqlite>) -> () {
//     //
// }
